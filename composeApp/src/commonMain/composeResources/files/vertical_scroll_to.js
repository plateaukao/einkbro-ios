// Jumps to an absolute vertical-rl position expressed as distance from the
// reading start (__TARGET__ is 0 for the start, __ebMax() for the end).
(function() {
    __VERTICAL_SCROLL_HELPERS__
    __ebSetFromStart(__TARGET__);
})();
